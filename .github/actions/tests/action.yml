name: 'Sonarqube analysis'
description: 'Run job to analys the PR with SonarQube'

runs:
  using: "composite"
  steps:
    - name: Run tests
      run: ./gradlew --build-cache test jacocoTestReport
      shell: bash